apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-review
  namespace: shopping-mall
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shop-review
  template:
    metadata:
      labels:
        app: shop-review
    spec:
      containers:
        - name: shop-review
          image: shopping-mall-shop-review:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8084
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql:13306/shop_review?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_PASSWORD
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: "kafka:19092"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: 500m
              memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: shop-review
  namespace: shopping-mall
spec:
  selector:
    app: shop-review
  ports:
    - port: 18084
      targetPort: 8084
